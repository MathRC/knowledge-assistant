# This file lists all environment variables required to run the application.
# Sensitive values should be placed in a separate, uncommitted .env file.

# --- Infrastructure Configuration ---
# Default connection settings for the local Weaviate Docker service.
WEAVIATE_HOST=weaviate
WEAVIATE_PORT=8080

# --- AWS Bedrock Credentials ---
# Required for connecting to the Bedrock API service. 
# Best practice is to use IAM Roles in a deployed cloud environment.
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=